# Dev-Test-V1.0
This short Console Application in C# allows a user to enter a date, add some days and show the new date taking the number of days in each month into account, along with any leap year.

It doesn't use any of the System.Date types, only string and basic arithmetic processing.
